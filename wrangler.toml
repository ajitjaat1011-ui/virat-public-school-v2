name = "virat-public-school-v2"
compatibility_date = "2025-05-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "./public"

# Environment variables are baked into the worker (see functions/lib/turso.js FALLBACK)
# For production, override via Cloudflare Pages env vars or `wrangler pages secret put`.
[vars]
TURSO_URL = "libsql://virat-public-school-ajitjaat1011-ui.aws-ap-south-1.turso.io"
